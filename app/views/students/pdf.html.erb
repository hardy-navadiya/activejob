Student id is:<%= @student.id %>
<table class="table">
  <thead>
    <tr>
      <th scope="col">Title</th>
      <th scope="col">Body</th>
      <th scope="col">Image data</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row"><%= @student.title %></th>
      <td><%= @student.body %></td>
      <td><%= @student.image_data %></td>
    </tr>
  </tbody>
</table>